# Native library search path for locally provisioned payloads (e.g. libsonic
# for operant-tts). NOT tracked in this repo: provision the payload locally
# (see local/), or set this path on each machine. An absent directory is
# harmless unless a crate actually links against something inside it.
[build]
rustflags = ["-L", "native=local/lib"]
